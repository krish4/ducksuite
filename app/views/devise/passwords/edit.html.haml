.main-content.login.new-password
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    .duck-pic= image_tag("login-duck.png")
    .login-form
      = f.hidden_field :reset_password_token
      .homepage-button
        = link_to "Log in", new_user_session_path
      %h1 Change password
      .input.block-field
        = f.label :password, "Type your new password"
        = f.password_field :password, autofocus: true, autocomplete: "off", autofocus: true
      .input.block-field
        = f.label :password_confirmation, "And confirm it"
        = f.password_field :password_confirmation, autocomplete: "off" 

      .under-form-box
        = f.submit "Change my password"